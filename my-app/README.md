# EPITA Web Advence Courses

This is all the project done during the sessions

## Install the project

`$ npm install`

Create a file `.env.local` with those constantes

`REACT_APP_API_URL=`

## Start the project

`$ npm start`